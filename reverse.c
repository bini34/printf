/**
* print_reversed_String- Prints string reverswise
* @strng: input string to be printed
*/
void print_reversed_String(const char *strng)
{
int length = strlen(strng);
for (int x = length - 1; x >= 0; x--)
printf("%c", stiirng[x]);
}
